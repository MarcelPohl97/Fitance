import React from 'react';

const ScreenTitle = ({title}) => {
    return (
        <>
            <h1 className="text-left text-6xl text-white font-bold">{title}</h1>
        </>
    )
}

export default ScreenTitle;